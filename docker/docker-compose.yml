version: '3'
services:
  lookups-processor-es:
    image: lookups-processor-es:latest
    build:
      context: ../
      dockerfile: docker/Dockerfile
    env_file:
      - api.env
    network_mode: "host"
